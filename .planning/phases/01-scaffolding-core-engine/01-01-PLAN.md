---
phase: 01-scaffolding-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "tailwind.config.ts", "src/app/layout.tsx", "src/app/globals.css", "src/app/page.tsx", "components.json"]
autonomous: false
requirements: ["PH1-SCAFFOLD", "PH1-THEME", "PH1-DEPLOY"]
user_setup:
  - service: Vercel
    why: "Continuous deployment for the project scaffold."
    env_vars: []
    dashboard_config:
      - task: "Connect repository to Vercel"
        location: "Vercel Dashboard -> Add New -> Project"
must_haves:
  truths:
    - "Application loads with a pixel font"
    - "The palette matches muted-gold/deep-green"
    - "A public Vercel URL is accessible"
  artifacts:
    - path: "tailwind.config.ts"
      provides: "Theme configuration"
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Press Start 2P"
      via: "next/font/google"
---

<objective>
Initialize the Next.js project scaffold, configure the custom pixel-art theme, and establish the deployment pipeline to Vercel.

Purpose: Provides the visual and technical foundation for all subsequent features.
Output: A running Next.js app with a themed landing page, deployed to Vercel.
</objective>

<execution_context>
@C:/Users/sinha/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/sinha/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js and shadcn/ui</name>
  <files>package.json, components.json, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
    Initialize a new Next.js project using `npx create-next-app@latest .` with non-interactive flags and force:
    `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-git --force`

    Then, initialize shadcn/ui using the non-interactive command:
    `npx shadcn-ui@latest init -y`

    Update `src/app/page.tsx` with a simple "GTO Teacher" heading to verify the setup.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Run `npm run dev` and verify "GTO Teacher" is visible at localhost:3000</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Next.js app and shadcn/ui are initialized and building successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Pixel-Art Theme</name>
  <files>tailwind.config.ts, src/app/globals.css, src/app/layout.tsx</files>
  <action>
    1. Import Google Font 'Press Start 2P' in `src/app/layout.tsx` using `next/font/google`.
    2. Extend `tailwind.config.ts` with:
       - fontFamily: { pixel: ['var(--font-press-start-2p)', 'cursive'] }
       - colors: { 
           'tavern-gold': '#D4AF37', 
           'tavern-green': '#1B4D3E',
           'tavern-dark': '#0D1B1E'
         }
    3. Update `src/app/globals.css` to set the default background to `tavern-dark` and text to `tavern-gold`.
    4. Configure shadcn/ui components in `tailwind.config.ts` to use `border-2` and `rounded-none` for a blocky pixel look.
    5. Add a "pixel-border" utility class in `globals.css` that implements a custom double-border look common in RPGs.
  </action>
  <verify>
    <automated>grep -q "tavern-gold" tailwind.config.ts && grep -q "Press Start 2P" src/app/layout.tsx</automated>
    <manual>Check the dev server to see the pixel font and colors applied to the landing page.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Theme tokens and fonts are correctly configured and visible in the UI.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Vercel Deployment</name>
  <what-built>Initial project scaffold with custom theme.</what-built>
  <how-to-verify>
    1. Run `npx vercel` to deploy the project to Vercel (or connect via Git).
    2. Confirm the deployment URL is active.
    3. Visit the URL and verify the "GTO Teacher" title appears with the 'Press Start 2P' font and tavern colors.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Check that the Next.js project is properly initialized, the pixel-art theme is applied, and the site is live on Vercel.
</verification>

<success_criteria>
- `npm run build` passes locally.
- Landing page uses the pixel font and Tavern colors.
- Site is live on Vercel.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-core-engine/01-01-SUMMARY.md`
</output>
